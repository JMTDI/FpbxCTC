# ── Compiled binaries ─────────────────────────────────────────────────────────
*.exe
*.exe~
FpbxCTC-Setup.exe

# ── Generated build artifacts ─────────────────────────────────────────────────
# rsrc.syso is auto-generated from FpbxCTC.ico by the build scripts
rsrc.syso
# ICO is generated from FpbxCTC.png at build time
FpbxCTC.ico
# Browser extension icons are generated from FpbxCTC.png at build time
browser-extension/icons/
# Packaged zip
browser-extension.zip

# ── Installer staging copies (populated by build_installer.bat) ───────────────
installer/FpbxCTC.exe
installer/FpbxCTC.ico
installer/browser-extension/icons/

# ── Go toolchain cache / test output ──────────────────────────────────────────
*.test
*.out
vendor/

# ── Rust / Cargo ──────────────────────────────────────────────────────────────
installer/target/
installer/Cargo.lock

# ── User config (stored in %APPDATA%, never in the repo) ──────────────────────
config.json

# ── CTC local/hairpin DID config (real numbers/domains, never in the repo) ────
pbx_files/ctc_local_config.lua

# ── OS clutter ────────────────────────────────────────────────────────────────
Thumbs.db
.DS_Store
desktop.ini

# ── Editor / IDE ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
