-- ─────────────────────────────────────────────────────────────────────────────
-- CTC local/hairpin DID config (EXAMPLE)
--
-- Copy this file to "ctc_local_config.lua" (same directory) on your actual
-- FreeSWITCH server and fill in your own numbers/domains. That filename is
-- git-ignored so real DIDs/domains never get committed to a public repo.
--
-- Map each agent number that is actually one of YOUR OWN PBX's DIDs (routed
-- back to this same server via an inbound route) to the FusionPBX domain its
-- inbound route lives under. See ctc.lua's "Local/hairpin DID handling"
-- comment block for why this is needed.
-- ─────────────────────────────────────────────────────────────────────────────
return {
    -- ["2075551234"] = "example.com",
}
